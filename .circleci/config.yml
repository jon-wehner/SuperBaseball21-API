version: 2.1

orbs:
  python: circleci/python@2.0.3

jobs:
  build-and-test:
    docker:
      - image: cimg/python:3.10.4
    steps:
      - checkout
      - run:
          command: |
            pipenv run pytest --version
          name: Run tests
workflows:
  test:
    jobs:
      - build-and-test
